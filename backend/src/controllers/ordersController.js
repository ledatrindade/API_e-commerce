const express = require('express');
const ordersModel = require('../models/ordersModel');
const router = express.Router();

//em desenvolvimento
app.get('/', request, response) => response.status(200).send('Enviado')

module.exports = router;
